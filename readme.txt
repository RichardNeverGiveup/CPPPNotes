bugs directory contains initial trial for using same name const variable in different .cpp files.
multifiles directory contains trial for separating header files and cpp files. 
g++ is not like vs, we need to specify linking process. only use g++ main.cpp will not work.
